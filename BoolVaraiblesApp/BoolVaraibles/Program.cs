namespace BoolVaraibles;
class Program
{
    static void Main(string[] args)
    {
        //true or false
        bool isComplete = false;
        //isComplete = !isComplete;
        Console.WriteLine(isComplete);
        Console.WriteLine(!isComplete);
        Console.ReadLine();
    }
}

